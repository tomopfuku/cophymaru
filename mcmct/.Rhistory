t = read.table("test.mcmc"0
t = read.table("test.mcmc")
m = as.mcmc(t)
require(coda)
m = as.mcmc(t)
traceplot(m[,2])
traceplot(m[,3])
traceplot(m[,4])
traceplot(m[,5])
traceplot(m[,6])
traceplot(m[,7])
traceplot(m[,8])
traceplot(m[,9])
traceplot(m[,0])
traceplot(m[,10])
effectiveSize(mcmc)
rejectionRate(m)
quit()
t
t = read.table("test.mcmc")
t = mcmc(t)
require(coda)
t = mcmc(t)
rejectionRate(t)
quit()
